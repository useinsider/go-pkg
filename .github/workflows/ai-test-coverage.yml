name: AI Test Coverage
on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  issue_comment:
    types: [created]

permissions:
  contents: read
  pull-requests: write
  issues: write
  statuses: write

jobs:
  coverage:
    uses: useinsider/ai-test-coverage/.github/workflows/ai-test-coverage.yml@main
    secrets: inherit
